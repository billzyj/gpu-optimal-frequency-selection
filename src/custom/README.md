# Custom Algorithms

This directory is dedicated to your own DVFS algorithms.

1. Implement new methods in `my_algo`.
2. Use `templates` as scaffolding for additional algorithm variants.
3. Keep interfaces compatible with the shared runner in `src/common`.
