# Proposed Method Config

Reserved config space for the user's proposed method.

## Rules

1. Do not store comparison-method reproduction configs here.
2. Record every user-facing config key before using it in experiments.
3. Keep exploratory tuning notes separate from final run configs.
4. When the proposed method becomes registered, document its
   `POLICY_NAME`, required keys, optional keys, and default values here.
